[project]
name = "solinpy"
version = "0.1.7"
description = "Python SDK for Solana"
authors = [{ name = "SolPy Team" }]
requires-python = ">=3.10"
dependencies = [
    "solders>=0.27.1",
    "solana>=0.36.11",
    "mnemonic>=0.21",
    "requests>=2.31.0",
    "httpx>=0.27.0"
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = [
    "solinpy.tests.*",
    "solinpy.client.test_client",
    "solinpy.client.test_async_client",
    "solinpy.client.rpc_mock"
]
ignore_errors = true

[tool.pytest.ini_options]
markers = [
	"integration: marks tests that perform real network calls",
]
